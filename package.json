{
  "name": "vercel-dns-proxy",
  "version": "1.0.0",
  "description": "Vercel Edge DoH DNS Proxy (Pure JS)",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "dns-packet": "^6.0.1",
    "base64-arraybuffer": "^1.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
